# Automated-Tweet-to-Image-Generator

This is a web app that lets users turn any public tweet into a downloadable image. The user provides a tweet link, and the app fetches the tweet's content using Twitter's oEmbed API. It then renders the tweet in a headless browser (using Puppeteer or Playwright), and returns the image. Users can then view, download, or share the image.
